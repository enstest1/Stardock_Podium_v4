# Optional: Coqui TTS XTTS v2 for cloning from voices/*/speaker_wav samples.
# Requires Python 3.9–3.11 (not 3.12). On RunPod use: bash scripts/setup_xtts_venv.sh
# Large first-time model download; use a CUDA torch wheel on GPU hosts (RunPod).
# Install after base requirements: pip install -r requirements.txt -r requirements-voice-clone.txt
#
# If versions conflict with Kokoro's torch, prefer installing TTS in a separate
# venv or align torch builds per Coqui's docs.
TTS==0.22.0
# TTS 0.22 is incompatible with transformers>=4.48 (BeamSearchScorer import).
# Pin first; re-run ``pip install -r requirements-voice-clone.txt`` or
# ``pip install "transformers==4.46.2" --force-reinstall`` after TTS on existing venvs.
transformers==4.46.2
